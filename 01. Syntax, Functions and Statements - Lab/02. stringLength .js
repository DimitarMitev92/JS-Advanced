function stringLength(firstArg, secondArg, thirdArg) {
    console.log(firstArg.length + secondArg.length + thirdArg.length);
    console.log(Math.floor((firstArg.length + secondArg.length + thirdArg.length)
        / 3));
}